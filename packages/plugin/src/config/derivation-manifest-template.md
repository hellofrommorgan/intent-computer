---
_comment: "Phase 7: This template is generated by /setup as ops/derivation-manifest.md. All {{placeholder}} values are replaced during generation based on the derivation conversation."
engine_version: "intent-computer-0.1.0"
platform: opencode
kernel_version: "1.0"

# 8 configuration dimensions
granularity: "{{granularity}}"
organization: "{{organization}}"
linking: "{{linking}}"
processing: "{{processing}}"
navigation: "{{navigation}}"
maintenance: "{{maintenance}}"
schema: "{{schema}}"
automation: "{{automation}}"

# Active feature blocks
active_blocks:
  - wiki-links
  - processing-pipeline
  - schema
  - maintenance
  - self-evolution
  - session-rhythm
  - templates
  - ethical-guardrails
  - atomic-notes
  - personality

# Vocabulary map — all 26 skill commands in domain language
# Universal → domain-specific
vocabulary:
  # Canonical runtime keys consumed by SKILL.md templates
  domain: "{{domain}}"
  notes: "{{folder_notes}}"
  inbox: "inbox"
  archive: "archive"
  note: "{{note_singular}}"
  topic_map: "topic map"
  topic_map_plural: "topic maps"
  topic_maps: "topic maps"
  reduce: "{{verb_reduce}}"
  reflect: "{{verb_reflect}}"
  reweave: "{{verb_reweave}}"
  verify: "verify"
  rethink: "rethink"
  notes_collection: "{{notes_collection}}"
  extraction_categories: "{{extraction_categories}}"
  architect: "architect"

  # Folder names
  folder_notes: "{{folder_notes}}"
  folder_inbox: "inbox"
  folder_archive: "archive"
  folder_self: "self"
  folder_ops: "ops"

  # Note type
  note_singular: "{{note_singular}}"
  note_plural: "{{note_plural}}"

  # Map/MOC type
  map_singular: "map"
  map_plural: "maps"

  # Process verbs
  verb_reduce: "{{verb_reduce}}"
  verb_reflect: "{{verb_reflect}}"
  verb_reweave: "{{verb_reweave}}"

  # Command names (user-facing slash commands)
  cmd_reduce: "{{cmd_reduce}}"
  cmd_reflect: "{{cmd_reflect}}"
  cmd_reweave: "{{cmd_reweave}}"
  cmd_verify: "verify"
  cmd_validate: "validate"
  cmd_seed: "seed"
  cmd_process: "process"
  cmd_tasks: "tasks"
  cmd_stats: "stats"
  cmd_graph: "graph"
  cmd_next: "next"
  cmd_learn: "learn"
  cmd_remember: "remember"
  cmd_rethink: "rethink"
  cmd_refactor: "refactor"
  cmd_help: "help"
  cmd_health: "health"
  cmd_ask: "ask"
  cmd_recommend: "recommend"
  cmd_architect: "architect"
  cmd_reseed: "reseed"
  cmd_upgrade: "upgrade"
  cmd_tutorial: "tutorial"
  cmd_add_domain: "add-domain"

platform_hints:
  context_type: "opencode-plugin"
  semantic_search_tool: "mcp__qmd__vector_search"
  autoapprove:
    - "mcp__qmd__search"
    - "mcp__qmd__vector_search"
    - "mcp__qmd__deep_search"
    - "mcp__qmd__get"
    - "mcp__qmd__multi_get"

personality:
  warmth: "high"
  directness: "high"
  formality: "casual"
  emotional_attentiveness: "high"
---

# Derivation Manifest

Generated by intent-computer /setup on {{date}}.

This file is read by every skill at invocation time to get domain vocabulary
and platform configuration. Do not edit manually — use /arscontexta:reseed to
re-derive from first principles.
