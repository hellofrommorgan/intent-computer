{
  "_comment": "Phase 7: This template is generated by /setup into the user's vault root as opencode.json. Variables like {{qmd_enabled}} are replaced during generation.",
  "$schema": "https://opencode.ai/config.json",
  "plugin": [
    "intent-computer"
  ],
  "mcp": {
    "_comment_qmd": "qmd block is included only when semantic search is enabled (config.yaml automation includes qmd)",
    "qmd": {
      "type": "local",
      "command": "qmd",
      "args": ["mcp"]
    }
  },
  "permission": {
    "read": "allow",
    "edit": "allow",
    "write": "allow",
    "bash": {
      "*": "allow",
      "rm -rf *": "ask",
      "git push --force *": "ask",
      "sudo *": "deny"
    }
  }
}
