{
  "name": "intent-computer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The intent computer — a five-layer cognitive architecture for agent computing",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "sync": "node scripts/sync-skills.js",
    "test": "pnpm run test:architecture && pnpm run test:regressions && pnpm run test:runtime && pnpm run test:fork",
    "test:architecture": "npx tsx scripts/test-architecture.ts",
    "test:fork": "npx tsx scripts/test-fork.ts",
    "test:runtime": "npx tsx scripts/test-runtime.ts",
    "test:regressions": "npx tsx scripts/test-regressions.ts",
    "controller": "npx tsx scripts/controller.ts"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
